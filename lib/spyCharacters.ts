import { Character } from './types';
import { CHARACTERS } from './characters';

// Select 4 characters for Who is the Spy (Aigerim, Daniyar, Zhanna, Erbol)
export const SPY_CHARACTERS: Character[] = CHARACTERS.slice(0, 4);
